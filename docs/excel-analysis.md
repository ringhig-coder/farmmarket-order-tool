# 발주요청서 샘플 파일 구조 분석 (2026-09-16 기준)

분석 대상 (`/Users/dede/Desktop/Claude/data/samples/260916/`):

1. `무무식탁_해남고구마식품_발주요청서_260916.xlsx`
2. `이웅식품_무무식탁_발주요청서_260916.xlsx`
3. `무무식탁 26.09.16.xlsx`

> 참고: 요청서에 언급된 260729/260508/260915/260909 등 과거 회귀 테스트 샘플 파일은 이 컴퓨터에서 찾지 못했고, 대신 오늘(260916) 실제 발주 파일 3개를 전달받아 분석했습니다. 과거 정답과의 1원 단위 회귀 검증은 아직 못했고, 대신 오늘 파일을 마스터 단가와 대조해 직접 검증했습니다 (아래 "검증" 참고). 과거 샘플 파일을 주시면 정식 회귀 테스트로 추가하겠습니다.

---

## 1. `무무식탁_해남고구마식품_발주요청서_260916.xlsx` — "박스수량형" 포맷

- 시트: `Sheet1`, 헤더는 **1행**, 데이터는 **2행부터** (118행까지 있지만 실제 데이터는 1건뿐, 나머지는 빈 행)
- 헤더: `받는분성명 | 받는분주소(전체, 분할) | 받는분전화번호 | 받는분기타연락처 | 박스수량 | 품목명 | 배송메모 | 구매자명`
- 실제 데이터 1행: 받는분성명=정원서, 박스수량=2, 품목명="해남에서 만든 반시고구마 60g 10봉"
- **수취인 1명 = 배송지 1곳**으로 취급 (수취인명+주소+전화번호 조합 기준)
- 이 포맷을 쓰는 업체는 파일명 접두어(`무무식탁_XXX_발주요청서`)의 `XXX`로 1차 식별하고, 품목명이 마스터에서 해당 업체 소속으로 확인되는지 교차검증한다.
- 파서: `BoxQuantityParser` (`src/farmmarket/parsers/box_quantity.py`)

### 계산 검증 (마스터 대조, 사람이 확인한 정답은 아니고 마스터 단가 기준 직접 계산)

- 품목명 "해남에서 만든 반시고구마 60g 10봉" → 마스터의 "해남에서말린 고구마말랭이 60g 10봉"과 이름이 다름 (제품 별칭 등록, `docs/master-analysis.md` 항목 4 참고)
- 공급가(택전) 15,500원 × 2박스 = 31,000원
- 배송지 1곳 × 배송비 3,000원 = 3,000원
- **합계 34,000원**, 계좌: 광주 630-107-310371 (해남고구마식품)

---

## 2. `이웅식품_무무식탁_발주요청서_260916.xlsx` — "사전계산형" 포맷

- 시트: `Sheet1`
- **1행**: 요약 행 — `발주처`(D열="팜마켓(무무식탁)"), `발주일`(G열=2026-09-16), `합계`(L열=31200, M열=3300, N열=34500) — 이 "합계"는 전체 발주서 합계이며 개별 상품 행과는 별개의 요약 셀
- **2행**: 실제 컬럼 헤더 — `연번 | 운송장번호 | 주문자 | 주문자연락처 | 수령자 | 수령자 연락처 | 수령지 우편번호 | 수령지 주소 | 배송메모 | 주문상품명 | 단가A | 수량B | 합계C=AxB | 배송비D | 합계C+D`
- **3행부터 데이터**: 1건만 실제 값이 있고 나머지는 빈 뼈대 행 (2행목은 0으로 채워진 빈 합계, 3행 이후는 완전히 빈 값 — 파서는 상품명이 없는 행은 무시해야 함)
- 이 업체는 **자체적으로 단가·합계·배송비를 이미 계산**해서 보내주는 특이 포맷 → `PrecomputedParser`가 파일에 적힌 숫자를 그대로 신뢰하지 않고, **마스터의 공급가(택전)·택배비와 반드시 대조**해서 일치할 때만 통과시키고 다르면 오류를 낸다 (마스터가 진실의 원천이라는 원칙 유지).

### 실제 데이터 1건

- 주문상품명: "스틱 참기름 10개입" (마스터 "스틱 참기름(5ml x 10ea)"과 이름은 다르지만 단가 10,400원 일치 → 별칭 등록)
- 단가 10,400 × 수량 3 = 32,100... **아님, 실제 셀 값은 31,200**(10,400 × 3 = 31,200이 맞음, 수식 오타 아님. 확인: 10,400 × 3 = 31,200 ✅)
- 배송비 3,300원 (마스터 택배비 3,300원과 일치)
- **합계 34,500원**, 계좌: 농협 301-0290-1706-71 (농업회사법인이웅식품유한회사)
- 파일 1행 요약값(31200/3300/34500)과 계산 결과가 정확히 일치함을 확인 → 이 파일 안에서 자체 검산도 가능 (이중 검증 포인트로 활용)

---

## 3. `무무식탁 26.09.16.xlsx` — 원본 라인아이템형 (다모식품, ✅ 해결됨)

- 시트: `sheet1`, 헤더: `운송장번호 | 주문번호 | 수하인명 | 수하인기본주소 | 수량 | 상품명 | 특기사항 | 고객메시지`
- 6건의 개별 주문 라인. 상품명이 전부 **"더 맛난 반시 고구마 말랭이 60g x 20봉" / "100g x 10봉"** 형태.
- **2026-09-14 다모식품 샘플로 확인**: 이 상품명은 다모식품이 플랫폼에서 내려받는 원본 주문 파일 특유의 표기이며, 마스터의 "반시 고구마말랭이 60g 10봉" 등과 같은 상품입니다. `product_aliases`에 등록했습니다.
- 파일명에 업체 힌트가 없는 원본 export이지만, 상품명이 등록된 별칭과 정확히 일치하면 업체를 특정할 수 있도록 엔진을 보강해서 이제 정상 계산됩니다.
- 파서: `RawLineItemParser` (`src/farmmarket/parsers/raw_line_item.py`)

---

## 4. `무무식탁_다모식품_발주요청서_260914.xlsx` — "원본 라인아이템형" (박스형과 다른 다모식품 전용 포맷)

- 시트: `Sheet1`, 헤더: `우편번호 | 주소 | 상품명 | 수량 | 수령자명 | 수령자연락처1 | 수령자연락처2 | 주문자 | 특기사항 | 운임구분 | 운임 | 주문번호 | 그룹송하주코드 | 고객메시지 | 배송메모`
- 총 522행 중 실제 주문은 22건 (나머지는 완전히 빈 행이거나, 상품명만 드롭다운으로 남아있고 수취인/주소/수량이 전부 빈 "빈 템플릿 행" — 이런 행은 실제 주문이 아니므로 조용히 건너뜀. 단, 수취인/주소는 있는데 수량만 빠진 경우는 진짜 오류로 보고 계산을 막음)
- 한 행 = 수취인 한 명에게 보낼 상품 한 종류, 수량은 그 상품(팩) 개수 그대로 (박스수량형과 동일한 의미, 컬럼 이름만 다름)
- 파서: `RawLineItemParser` (품목명 컬럼명이 파일마다 다를 수 있어 별칭 목록으로 매칭: `상품명`/`주문상품명`이 아니라 `상품명`, 수취인은 `수령자명`/`수하인명` 등)
- 검증: 60g 20봉 1개 + 60g 10봉 9개 + 100g 10봉 13개, 23개 배송지(전원 다른 주소) → 상품합계 318,000원 + 배송비 23×2,500=57,500원 = **375,500원**

---

## 5. `무무식탁_주줌_발주요청서_260914.xlsx`, `무무식탁_한칼식품_발주요청서_260914.xlsx` — "수취인+옵션정보형"

- 시트명이 `발주발송관리`. **1행은 "OO 발주요청서 26/09/14" 제목**, 2행이 실제 헤더: `수취인명 | 수취인연락처1 | 수취인연락처2 | 배송지 | 배송메세지 | 옵션정보 | 수량`
- `옵션정보` 컬럼이 상품명 역할을 함
- 파서: `SimpleOptionParser` (`src/farmmarket/parsers/simple_option.py`)
- 주줌 검증: "한입에 반한 반시고구마 60g x 10봉" 3개, 1명 → 12,000×3 + 2,500 = **38,500원** (마스터 상품명과 파일 표기가 정확히 일치, 별칭 불필요)
- 한칼식품 검증: "땡초 비빔된장 실속형 (400g)" 1개, 1명 → **택전(8,380)+배송비(4,000)=12,380원이 아니라 택포 11,700원이 정답** (섹션 17에서 사용자가 명시한 특이 규칙과 정확히 일치). `pricing_mode_overrides`에 한칼식품을 "택포 직접 사용" 업체로 등록했습니다.

---

## 6. `무무식탁_해남고구마식품_발주요청서_260914.xlsx` — 박스수량형 (7건, 여러 배송지)

- 260916 샘플과 같은 포맷. 7건 모두 "해남에서 만든 반시고구마 60g 10봉"(별칭 등록됨), 7명 배송지, 박스수량 합계 17개
- 검증: 15,500×17 + 3,000×7 = 263,500 + 21,000 = **284,500원**

---

## 7. `무무식탁_밀양한천_발주요청서_260805.xlsx`, `무무식탁_하영이네_발주요청서_260915.xlsx` — 수취인+옵션정보형 (컬럼 그대로 일치)

- `simple_option` 파서 그대로 사용, 상품명도 마스터와 정확히 일치해서 별칭 불필요
- 밀양한천: "프리미엄 넛츠양갱" 1개, 택전 11,200 + 배송비 4,000(1호 행에서 forward-fill) = **15,200원**
- 하영이네: "순한맛 떡갈비 120g\*4" 1개 = **9,720원** (섹션 16 정답과 일치)

## 8. `나는농부_팜마켓_발주요청서_260915.xlsx`, `나는농부_신안산업_발주요청서_260803.xlsx` — 수취인+옵션정보형 (컬럼명이 조금 다른 변형)

- 헤더가 `옵션정보` 대신 `상품 정보`/`상품정보`, `수취인연락처1` 대신 `수취인연락처`인 변형이 있어 `simple_option` 파서에 별칭을 추가했습니다.
- 팜마켓: "존디어 미션오일 20L"(파일 표기, 마스터는 "⭐존디어 HY-Gard 20L (유압/미션오일 겸용)" — 별칭 등록) 2개 = 139,000×2 = **278,000원** (섹션 15/17 정답과 일치)
- 신안산업: "발판 사다리 1단/2단"(마스터는 공백 없이 "발판사다리" — 별칭 등록), "SAB3 2륜"(마스터는 "SAB3- 2륜" — 별칭 등록)은 정상 계산(541,200원). **"평사다리 6단"은 마스터에 "평사다리 4자/5자/6자/7자"만 있고 "6단"이라는 표기가 없어서 계산을 막았습니다** — "6단"이 "6자"와 같은 것인지 확인 부탁드립니다 (길이 단위가 달라 보여서 임의로 연결하지 않았습니다).

## ⚠️ 계산 엔진에서 발견해 수정한 버그 (참고용)

별칭이 2개 이상 등록된 업체에서, 정말 등록되지 않은 새로운 상품명이 들어왔을 때 **엔진이 그 업체의 아무 별칭이나 골라 잘못 매칭하는 버그**가 있었습니다 (신안산업 테스트 중 "평사다리 6단"이 엉뚱하게 "발판사다리 1단"으로 합산되는 것을 발견). 원인은 `engine.py`의 별칭 대조 로직이 별칭의 "값" 목록을 무조건 순회해서 매칭 처리해버린 것이었고, 원래 있던 정확한 키 대조 로직(`resolve_product_alias`)과 중복되면서 버그가 있는 쪽이 실행됐습니다. 중복 로직을 제거해서 고쳤고, 회귀 테스트(`test_unmapped_product_within_aliased_company_is_not_silently_matched`)를 추가했습니다.
